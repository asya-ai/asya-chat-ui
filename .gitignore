.env
.venv
node_modules
dist
dist-ssr
*.local
.pnpm-store
*.db
postgres_data

.vscode
.DS_Store
**/.DS_Store

data

.continue
scraper/.cache

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
.cursor